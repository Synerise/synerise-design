import React from 'react';

import Sortable from '@synerise/ds-sortable';

const stories = {
  default: () => ({}),
};

export default {
name: 'Components/Sortable',
  config: {},
  stories,
  Component: Sortable,
}
