import * as React from 'react';

import Wizard from '@synerise/ds-wizard';

const stories = {
  default: () => ({}),
};

export default {
name: 'Components/Wizard',
  config: {},
  stories,
  Component: Wizard,
}
