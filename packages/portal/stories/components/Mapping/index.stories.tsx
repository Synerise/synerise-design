import React from 'react';

import Mapping from '@synerise/ds-mapping';

const stories = {
  default: () => ({}),
};

export default {
name: 'Components/Mapping',
  config: {},
  stories,
  Component: Mapping,
}
