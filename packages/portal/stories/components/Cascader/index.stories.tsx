import * as React from 'react';

import Cascader from '@synerise/ds-cascader';

const stories = {
  default: () => ({}),
};

export default {
  name: 'Components|Cascader',
  config: {},
  stories,
  Component: Cascader,
}
