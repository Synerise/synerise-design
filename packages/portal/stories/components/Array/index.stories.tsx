import React from 'react';

import Array from '@synerise/ds-array';

const stories = {
  default: () => ({}),
};

export default {
name: 'Components/Array',
  config: {},
  stories,
  Component: Array,
}
