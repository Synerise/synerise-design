import styled from 'styled-components';

export const UserInfo = styled.div`
  overflow: hidden;
  text-overflow: ellipsis;
  .name {
    font-weight: 500;
    margin-right: 4px;
  }
  .email {
    font-weight: 400;
  }
`;
