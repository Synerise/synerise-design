import * as React from 'react';

import Alert from '@synerise/ds-alert';

const stories = {
  default: () => ({}),
};

export default {
  name: 'Components|Alert',
  config: {},
  stories,
  Component: Alert,
}
