import { Meta, Story, Canvas , ArgsTable} from '@storybook/addon-docs/blocks';
import Status from '@synerise/ds-status';
import { TagShape } from '@synerise/ds-tags';
import Tags from '@synerise/ds-tags/dist/Tags'


# Komponent Status

Let's define a story for our `Cruds` component:

<Story id="components-pills--status-basic" />

We can drop it in a `Canvas` to get a code snippet:

We can even preview multiple Stories in a block. This
gets rendered as a group but defines individual stories
with unique URLs, which is great for review and testing.
<Canvas>
    <Story id="components-pills--pill-small" />
</Canvas>

## Component API
<ArgsTable  />

