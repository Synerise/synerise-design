import * as React from 'react';

import Grid from '@synerise/ds-grid';

const stories = {
  default: () => ({}),
};

export default {
name: 'Components/Grid',
  config: {},
  stories,
  Component: Grid,
}
