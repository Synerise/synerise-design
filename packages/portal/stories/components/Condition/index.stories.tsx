import * as React from 'react';

import Condition from '@synerise/ds-condition';

const stories = {
  default: () => ({}),
};

export default {
name: 'Components/Condition',
  config: {},
  stories,
  Component: Condition,
}
