import * as React from 'react';

import Result from '@synerise/ds-result';

const stories = {
  default: () => ({}),
};

export default {
  name: 'Components|Result',
  config: {},
  stories,
  Component: Result,
}
