{
  "name": "@synerise/ds-core",
  "version": "0.0.3",
  "description": "Core Components for the Synerise Design System",
  "license": "ISC",
  "repository": "synerise/ds",
  "main": "dist/js/index.js",
  "scripts": {
    "build": "npm run build:js && npm run vars && npm run copy && npm run build:css",
    "build:css": "node ../../../scripts/style/less.js",
    "build:js": "babel --root-mode upward src --out-dir dist --extensions '.js,.ts,.tsx'",
    "build:watch": "npm run build:js -- --watch",
    "copy": "node ./build/copy.js",
    "prepublish": "npm run build",
    "test": "jest",
    "test:watch": "npm run test -- --watchAll",
    "vars": "node ./build/vars.js"
  },
  "sideEffects": false,
  "dependencies": {
    "antd": "*"
  },
  "peerDependencies": {
    "react": ">=16.9.0 < 17.0.0"
  },
  "devDependencies": {
    "less-vars-loader": "^1.1.0",
    "webpack": "^4.39.3"
  }
}
