{
  "name": "@synerise/ds-data-format",
  "version": "0.6.1",
  "description": "DataFormat UI Components and tools for the Synerise Design System",
  "license": "ISC",
  "repository": "Synerise/synerise-design",
  "main": "dist/index.js",
  "files": [
    "/dist",
    "CHANGELOG.md",
    "README.md",
    "package.json",
    "LICENSE.md"
  ],
  "publishConfig": {
    "access": "public"
  },
  "scripts": {
    "build": "npm run build:js && npm run build:css && npm run defs",
    "build:css": "node ../../../scripts/style/less.js",
    "build:js": "babel --delete-dir-on-start --root-mode upward src --out-dir dist --extensions '.js,.ts,.tsx'",
    "build:watch": "npm run build:js -- --watch",
    "defs": "tsc --declaration --outDir dist/ --emitDeclarationOnly",
    "pack:ci": "npm pack --pack-destination ../../portal/storybook-static/static",
    "prepublish": "npm run build",
    "test": "jest && yarn test:timezones",
    "test:timezone-ny": "TZ=America/New_York jest -t TimezoneTesting",
    "test:timezone-utc": "TZ=UTC jest -t TimezoneTesting",
    "test:timezone-waw": "TZ=Europe/Warsaw jest -t TimezoneTesting",
    "test:timezones": "yarn test:timezone-utc && yarn test:timezone-ny && yarn test:timezone-waw",
    "test:watch": "npm run test -- --watchAll",
    "types": "tsc --noEmit",
    "upgrade:ds": "ncu -f \"@synerise/ds-*\" -u"
  },
  "sideEffects": [
    "dist/style/*",
    "*.less"
  ],
  "types": "dist/index.d.ts",
  "dependencies": {
    "dayjs": "^1.8.36",
    "moment": "^2.30.1"
  },
  "peerDependencies": {
    "@synerise/ds-core": "*",
    "react": ">=16.9.0 <= 18.3.1",
    "react-intl": ">=3.12.0 <= 6.8"
  }
}
