import styled from 'styled-components';
import BaseDivider from '@synerise/ds-divider';

export const Wrapper = styled.div`
  padding: 0 12px;
`;

export const Divider = styled(BaseDivider)`
  && {
    margin: 8px 0;
  }
`;
