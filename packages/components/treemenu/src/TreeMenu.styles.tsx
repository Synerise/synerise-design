import styled from 'styled-components';
import Menu from '@synerise/ds-menu';

export const TreeMenu = styled(Menu)`
  background: #fff;
`;

export const TreeMenuContainer = styled.div`
  position: relative;
  background: #fff;
`;
